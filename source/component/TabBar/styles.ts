import { StyleSheet } from "react-native"

export default StyleSheet.create({
  tabBar: {
    flexDirection: "row",
    width: "100%",
    justifyContent: "space-around",
    height: 76,
    alignItems: "center",
  },
})
